
scan(5);
scan(if);

len();
len(47);
len(str1, str2);
len(pin[0]);

cmp(1);
cmp(str1);
cmp(str1, str2, str3);
cmp(5, "a");

print(5);
print(len());
print(len(5));
print(len(10, 20));

print(cmp());
print(cmp, len);
print([1, 2, 3]);